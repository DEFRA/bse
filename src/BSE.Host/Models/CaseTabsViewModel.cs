namespace BSE.Host.Models;

/// <summary>View model for the _CaseTabs partial — identifies active tab and RBSE for link generation.</summary>
public sealed record CaseTabsViewModel(string ActiveTab, string Rbse);
